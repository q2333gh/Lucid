# First-Time Setup

Run the setup script:
Assume using x86 ubuntu2204 OS with sudo permission 
```bash
sudo ./setup.sh
``
Then:
```bash

python build.py --new my_canister
python build.py --icwasm --examples adder
```

