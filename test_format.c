#include <stdio.h>
#include <stdlib.h>
int main() {
    int x = 10;
    int y = 20;
    if (x < y) {
        printf("x is less than y\n");
    } else {
        printf("x is greater than or equal to y\n");
    }
    return 0;
}
